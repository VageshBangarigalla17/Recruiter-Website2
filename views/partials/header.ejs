<!DOCTYPE html>
<html lang="en" x-data="{ dark: false, sidebarOpen: false }" :class="{ 'dark': dark }">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>HRMS Dashboard</title>

  <!-- Tailwind CSS -->
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">

  <!-- Icons & Alpine.js -->
  <script defer src="https://cdn.jsdelivr.net/npm/alpinejs@2.8.2"></script>
  <script defer src="https://unpkg.com/heroicons@1.0.6/dist/heroicons.min.js"></script>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/7.0.0/css/all.min.css" />
  <style>
  table th, table td {
    border-bottom: 1px solid #e5e7eb;
    border-right: 1px solid #e5e7eb;
    padding: 0.5rem 0.75rem;
    text-align: start;
    vertical-align: middle;
    white-space: nowrap;
  }
  table th:last-child, table td:last-child {
    border-right: none;
  }
  table tr:last-child td {
    border-bottom: none;
  }
</style>
</head>
<body class="h-screen flex bg-gray-100 dark:bg-gray-900 text-gray-800 dark:text-gray-200 overflow-hidden">

  <!-- include the sidebar partial -->
  <%- include('./sidebar') %>

  <!-- main content area -->
  <div class="flex-1 flex flex-col overflow-hidden">

    <!-- HEADER -->
    <header class="flex items-center justify-between bg-white dark:bg-gray-800 shadow px-4 sm:px-6 lg:px-8 py-3">
      <!-- left: hamburger (only on small) + brand -->
      <div class="flex items-center">
        <!-- mobile menu button -->
        <button
          @click="sidebarOpen = true"
          class="text-gray-600 dark:text-gray-300 focus:outline-none lg:hidden mr-2"
        >
          <svg class="w-6 h-6" fill="none" stroke="currentColor" stroke-width="2"
               viewBox="0 0 24 24" stroke-linecap="round" stroke-linejoin="round">
            <path d="M4 6h16M4 12h16M4 18h16"/>
          </svg>
        </button>
        <!-- logo/brand -->
        <a href="/" class="text-xl sm:text-2xl font-bold text-gray-800 dark:text-white">
          ADYAHR RECRUITMENT
        </a>

       
      </div>

      <!-- right: notifications, theme toggle, user -->
      <div class="flex items-center space-x-4">

         <a href="/" class="block px-4 py-2 hover:bg-gray-100 dark:hover:bg-gray-600 "><i class="fa-solid fa-house"></i>Home</a>
    <a href="/candidates" class="flex items-center px-3 py-2 rounded hover:bg-gray-200 dark:hover:bg-gray-700">
      <i class="fas fa-clipboard-list w-5"></i><span class="ml-2">Candidates</span>
    </a>
    <a href="/candidates/new" class="flex items-center px-3 py-2 rounded hover:bg-gray-200 dark:hover:bg-gray-700">
      <i class="fas fa-user-plus w-5"></i><span class="ml-2">Add Candidate</span>
    </a>

    <% if (user && user.role === 'admin') { %>
      <a href="/admin/recruiters" class="flex items-center px-3 py-2 rounded hover:bg-gray-200 dark:hover:bg-gray-700">
        <i class="fas fa-users w-5"></i><span class="ml-2">Recruiters</span>
      </a>
      <a href="/admin/recruiters/new" class="flex items-center px-3 py-2 rounded hover:bg-gray-200 dark:hover:bg-gray-700">
        <i class="fas fa-user-plus w-5"></i><span class="ml-2">Add Recruiter</span>
      </a>
    <% } %>
    
       </a>
        <!-- notifications (hidden on xs) -->
        <!-- <button class="hidden sm:inline-flex relative text-gray-600 dark:text-gray-300 hover:text-gray-900 dark:hover:text-white focus:outline-none">
          <svg class="w-6 h-6" fill="none" stroke="currentColor" stroke-width="2"
               viewBox="0 0 24 24" stroke-linecap="round" stroke-linejoin="round">
            <path d="M15 17h5l-1.4-1.4A2 2 0 0118 14V11a6 6 0 10-12 0v3a2 2 0 01-.6 1.4L4 17h5m6 0v1a3 3 0 11-6 0v-1"/>
          </svg>
          <span class="absolute top-0 right-0 w-2 h-2 bg-red-600 rounded-full"></span>
        </button> -->

        <!-- theme toggle -->
        <!-- <button
          @click="dark = !dark"
          class="text-gray-600 dark:text-gray-300 hover:text-gray-900 dark:hover:text-white focus:outline-none"
        >
          <template x-if="!dark">
            <svg class="w-6 h-6" fill="none" stroke="currentColor" stroke-width="2"
                 viewBox="0 0 24 24" stroke-linecap="round" stroke-linejoin="round">
              <path d="M12 3v1m0 16v1m8.6-9h-1M4.3 12h-1m15.1 6.1l-.7-.7M6.3 6.3l-.7-.7M18.3 6.3l-.7.7M6.3 17.7l-.7.7M12 5a7 7 0 100 14 7 7 0 000-14z"/>
            </svg>
          </template>
          <template x-if="dark">
            <svg class="w-6 h-6 text-yellow-400" fill="currentColor" viewBox="0 0 20 20">
              <path d="M10 2a1 1 0 011 1v1a1 1 0 11-2 0V3a1 1 0 011-1zM18 9a1 1 0 110 2h-1a1 1 0 110-2h1zM4 9a1 1 0 110 2H3a1 1 0 110-2h1zM12 16a4 4 0 100-8 4 4 0 000 8z"/>
            </svg>
          </template>
        </button> -->

        <!-- user menu -->
        <div class="relative" x-data="{ open: false }">
          <button
            @click="open = !open"
            class="flex items-center focus:outline-none space-x-2"
          >
            <% if (user && user.avatar) { %>
              <img src="<%= user.avatar %>" alt="Avatar"
                   class="w-8 h-8 sm:w-10 sm:h-10 rounded-full object-cover border-2 border-gray-300">
            <% } else { %>
              <img src="/assets/default-avatar.png" alt="Avatar"
                   class="w-8 h-8 sm:w-10 sm:h-10 rounded-full object-cover border-2 border-gray-300">
            <% } %>
            <svg class="w-4 h-4 text-gray-600 dark:text-gray-300" fill="none" stroke="currentColor"
                 viewBox="0 0 24 24" stroke-linecap="round" stroke-linejoin="round" stroke-width="2">
              <path d="M19 9l-7 7-7-7"/>
            </svg>
          </button>

          <div
            x-show="open"
            @click.away="open = false"
            class="origin-top-right absolute right-0 mt-2 w-48 bg-white dark:bg-gray-700 border border-gray-200 dark:border-gray-600 rounded-md shadow-lg py-1 z-50"
          >
            <!-- <a href="/" class="block px-4 py-2 hover:bg-gray-100 dark:hover:bg-gray-600">Home</a> -->
            <a href="/profile" class="block px-4 py-2 hover:bg-gray-100 dark:hover:bg-gray-600"><i class="fa-solid fa-pen-to-square"></i><span class="ml-2">Edit Profile</span></a>
            <a href="/change-password" class="flex items-center px-3 py-2 rounded hover:bg-gray-200 dark:hover:bg-gray-700"><i class="fas fa-lock w-5"></i><span class="ml-2">Change Password</span></a>
            <a href="/settings" class="block px-4 py-2 hover:bg-gray-100 dark:hover:bg-gray-600"><i class="fa-solid fa-gear"></i><span class="ml-2">Settings</span></a>
            <form action="/logout" method="POST">
              <button type="submit"
                      class="w-full text-left px-4 py-2 text-red-600 hover:bg-red-100 dark:hover:bg-red-900">
                🚪 Logout
              </button>
            </form>
          </div>
        </div>
      </div>
    </header>

    <!-- PAGE CONTENT WRAPPER -->
    <div class="flex-1 overflow-y-auto bg-gray-50 dark:bg-gray-900 p-4 sm:p-6 lg:p-8">
      <!-- your page’s main content goes here -->
